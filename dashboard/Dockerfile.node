FROM node:20-slim

WORKDIR /app

# Copy package files and install
COPY dashboard/package*.json ./
RUN npm install

# Copy dashboard source
COPY dashboard/ .

# Build the app (if production) or just run dev
CMD ["npm", "run", "dev", "--", "--host"]
